/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //    runtime: "experimental-edge",
  //  },
   reactStrictMode: true,
   swcMinify: true,
   appDir: true,
 reactStrictMode: true,
}

module.exports = nextConfig
